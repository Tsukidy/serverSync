# serverSync
serverSync is a simple script that will enable networking on the computer which was disabled, create a connection to a backup server, then use free file sync to perform a backup sync of the files at the location to the target.
